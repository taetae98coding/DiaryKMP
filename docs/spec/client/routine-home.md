# RoutineHome 화면 스펙

이 문서는 사용자가 공통 내비게이션의 `루틴` 목적지에서 사용하는 RoutineHome 화면의 내용과 행동을 다룬다. 주요 목적지의 구성과 이동, 공통 내비게이션 노출은 [TopLevelNavigation 스펙](./top-level-navigation.md)에서, 목록을 당겨 새로고침하는 행동과 동기화 진행 표시는 [새로고침 스펙](./sync-refresh.md)에서, 목록이 비어 있을 때의 안내는 [목록 빈 상태 스펙](./list-empty-state.md)에서, 루틴 목록과 루틴 추가를 함께 사용하는 배치는 [Routine 목록·상세 배치 스펙](./routine-list-detail.md)에서, 루틴을 작성하는 화면의 입력은 [RoutineAdd 화면 스펙](./routine-add.md)에서 다룬다.

디자인: [RoutineHome 화면 디자인](../../design/routine-home.md)

이번 범위는 RoutineHome에서 루틴 목록이 비어 있음을 알리고, 목록을 당겨 새로고침하고, 루틴 추가로 이동하는 것까지다. 루틴을 저장해 목록에 노출하는 것은 이번 범위에 포함하지 않는다.

## feature

### 진입

사용자는 공통 내비게이션에서 `루틴` 목적지를 선택해 RoutineHome 화면으로 이동한다.

### 루틴 목록

사용자는 RoutineHome에서 계정의 루틴을 목록으로 확인한다.

루틴을 저장하는 흐름을 아직 제공하지 않으므로 이번 범위에서 목록에 노출하는 루틴은 없다.

### 빈 상태

표시할 루틴이 하나도 없으면 목록 자리에 아직 루틴이 없으며 새로 추가할 수 있음을 알린다. 판정 시점, 빈 상태에서 할 수 있는 행동, 목록과의 전환은 [목록 빈 상태 스펙](./list-empty-state.md)을 따른다.

목록이 비어 있다는 이유로 루틴 추가와 당겨서 새로고침이 막히지 않는다.

### 새로고침

사용자는 목록을 아래로 당겨 계정 데이터를 서버와 다시 맞추도록 요청할 수 있다. 당기는 조작이 시작하는 동기화, 진행 표시 기준과 실패 처리는 [새로고침 스펙](./sync-refresh.md)을 따른다.

### 루틴 추가로 이동

사용자는 RoutineHome에서 루틴 추가를 선택해 [RoutineAdd 화면](./routine-add.md)으로 이동한다.

물리 키보드를 쓰는 환경에서는 추가 단축키를 눌러도 같은 루틴 추가로 이동한다. 키 조합은 [RoutineHome 화면 디자인](../../design/routine-home.md)의 `키보드 단축키`를 따른다. 루틴 추가를 목록에서 실행하지 않는 동안에는 단축키로도 이동하지 않는다.

루틴 추가는 목록의 내용과 무관하게 언제나 실행할 수 있다. 노출할 루틴이 없어 목록이 비어 있는 상태에서도 사용자는 루틴 추가로 이동할 수 있다.

목록과 상세를 함께 사용하는 환경에서는 상세 영역에 이미 루틴 추가가 놓이므로 목록에서 루틴 추가를 다시 실행하지 않는다. 그 기준은 [목록·상세 배치 공통 스펙](./list-detail-pane.md)의 `추가 진입 수단`을 따른다.

루틴 추가에서 뒤로 돌아오면 RoutineHome으로 돌아온다.

### 뒤로가기

사용자가 RoutineHome에서 뒤로가기를 실행하면 기본 목적지로 돌아간다. 주요 목적지 사이의 전환 이력은 [TopLevelNavigation 스펙](./top-level-navigation.md)의 `기본 목적지와 전환 이력`을 따른다.

## domain

### 목록에 노출하는 루틴

루틴의 노출 기준과 정렬 기준은 루틴을 저장하는 후속 범위에서 정한다.

이번 범위에서는 노출 기준을 만족하는 루틴이 없으므로 목록은 언제나 빈 상태로 판정한다. 판정 기준은 [목록 빈 상태 스펙](./list-empty-state.md)의 `빈 상태 판정 기준`을 따른다.

### 실행 경계

RoutineHome은 사용자가 이어서 작성하는 진행 상태를 갖지 않는다.

화면이 재생성되거나 앱이 백그라운드에 다녀오거나 앱을 다시 실행하면 그 시점의 목록을 다시 준비해 같은 기준으로 판정한다.
