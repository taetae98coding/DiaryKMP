# RoutineHome 화면 디자인

기준 스펙: [RoutineHome 화면 스펙](../spec/routine-home.md), [목록 빈 상태 스펙](../spec/list-empty-state.md), [새로고침 스펙](../spec/sync-refresh.md)

## 화면 구조

RoutineHome 화면은 `루틴` 주요 목적지의 첫 화면이므로 앱의 공통 내비게이션 영역과 함께 표시한다.

화면 상단에는 다국어 제목을 표시하는 상단 바를 둔다. 상단 바에는 뒤로가기 동작과 그 밖의 동작을 두지 않는다.

본문은 상단 바 아래 영역 전체를 채우는 루틴 목록 영역 하나로 두고, 오른쪽 아래에 루틴 추가를 실행하는 플로팅 액션 버튼을 겹쳐 표시한다. 버튼에는 추가를 뜻하는 더하기 아이콘을 표시한다.

## 루틴 추가 버튼

플로팅 액션 버튼은 목록의 상태와 관계없이 같은 자리에 같은 모양으로 표시하고, 비활성 표현을 사용하지 않는다.

목록과 상세를 함께 표시하는 동안에는 상세 영역이 이미 RoutineAdd이므로 이 버튼을 표시하지 않는다. 노출 기준은 [목록·상세 배치 공통 디자인](./list-detail-pane.md)의 `버튼 노출`을 따른다.

버튼을 누르면 [RoutineAdd 화면](./routine-add.md)으로 이동한다.

## 빈 상태

표시할 루틴이 하나도 없으면 목록 자리에 [목록 빈 상태 디자인](./list-empty-state.md)의 빈 상태를 표시한다. 이 목록은 루틴 아이콘과 아래 `문구와 접근성`의 제목 문구·보조 문구를 사용하며, 목록과 빈 상태 사이의 전환도 그 문서를 따른다.

빈 상태에서도 플로팅 액션 버튼은 같은 자리에 같은 모양으로 표시한다.

## 새로고침

본문 전체가 루틴 목록 영역이므로 본문 전체를 [새로고침 디자인](./sync-refresh.md)의 당김 새로고침 컨테이너로 감싼다. 목록 대신 빈 상태를 표시하는 동안에는 스크롤할 것이 없으므로 당김이 곧바로 시작된다.

진행 표시의 모양과 자리, 진행 중 조작 가능 여부는 [새로고침 디자인](./sync-refresh.md)을 그대로 따르며 이 화면에서 따로 정하지 않는다. 진행 표시는 본문 위쪽에, 플로팅 액션 버튼은 본문 오른쪽 아래에 놓이므로 두 요소는 겹치지 않는다.

## 적응형 배치

창 너비가 좁으면 RoutineHome 화면만 표시하고, 넓으면 루틴 목록과 상세 영역을 함께 표시한다. 함께 표시할 때의 배치와 버튼 노출은 [Routine 목록·상세 배치 디자인](./routine-list-detail.md)을 따른다.

본문은 시스템 상태 영역과 디스플레이 컷아웃에 가려지지 않는 자리에 두고, 목록의 마지막 항목이 플로팅 액션 버튼에 가리지 않게 배치한다.

## 키보드 단축키

물리 키보드 환경에서는 `Cmd` 또는 `Meta`와 `A`를 함께 눌러 [RoutineAdd 화면](./routine-add.md)으로 이동할 수 있다. 같은 조합을 쓰는 [TagHome 목록 디자인](./tag-home.md)과 같으므로 사용자는 화면이 달라도 같은 조합으로 항목을 추가한다.

루틴 추가 버튼을 표시하지 않는 동안에는 이 단축키도 제공하지 않는다.

## 문구와 접근성

한국어 환경과 그 외 기본 환경에서 다음 문구를 사용한다.

| 항목 | 한국어 | 그 외 기본 |
| --- | --- | --- |
| 상단 바 제목 | `루틴` | `Routine` |
| 루틴 추가 버튼 접근성 이름 | `루틴 추가` | `Add routine` |
| 빈 상태 제목 문구 | `아직 루틴이 없습니다` | `No routines yet` |
| 빈 상태 보조 문구 | `추가 버튼으로 새 루틴을 만들 수 있습니다` | `Use the add button to create a routine.` |

공통 내비게이션의 `루틴` 목적지 이름과 접근성 이름은 [TopLevelNavigation 디자인](./top-level-navigation.md)을 따른다.
