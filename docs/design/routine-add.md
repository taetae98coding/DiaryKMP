# RoutineAdd 화면 디자인

기준 스펙: [RoutineAdd 화면 스펙](../spec/routine-add.md)

화면 구조, 표시 영역과 소프트 키보드, 초점과 단축키의 공통 표현은 [항목 추가 화면 공통 디자인](./entity-add.md)을 따른다.

## 화면 구조와 적응형 배치

RoutineHome에서 진입하면 넓은 화면에서는 루틴 목록과 RoutineAdd 화면을 함께 표시하고, 좁은 화면에서는 RoutineAdd 화면만 표시한다. 함께 표시할 때의 구체적인 배치와 버튼 노출은 [Routine 목록·상세 배치 디자인](./routine-list-detail.md)을 따른다.

화면 상단에는 제목이 있는 상단 바를 둔다. RoutineAdd 화면을 단독 표시할 때만 뒤로가기 버튼을 표시한다. 상단 바에는 추가 동작을 실행하는 컨트롤을 두지 않는다.

본문에는 단일 줄 제목 입력과 설명 입력을 순서대로 세로로 배치하고 두 입력 사이에 [공통 구성요소 간격](./dimens.md)을 둔다. 두 입력은 모두 본문 가로 폭을 모두 사용한다. 추가 동작은 떠 있는 버튼으로 표시한다.

설명 입력의 구성과 표현은 [설명 입력 컴포넌트 디자인](./description-input.md)을 따른다.

배치는 창 높이와 기기 자세에 따라 달라지지 않는다.

## 추가 버튼

추가 동작은 본문 오른쪽 아래에 떠 있는 버튼으로 두고, 추가를 뜻하는 더하기 아이콘을 표시한다. 다른 추가 화면과 같은 자리에 같은 모양으로 두어 사용자가 화면마다 버튼을 다시 찾지 않게 한다.

기준 스펙의 `루틴 추가`에 따라 이번 범위에는 추가 동작이 없으므로, 버튼을 눌러도 화면과 입력 내용이 바뀌지 않고 진행 표시나 스낵바 피드백도 표시하지 않는다. 동작이 없다는 이유로 버튼을 비활성으로 표시하거나 숨기지는 않는다.

같은 이유로 이번 범위에서는 추가 동작을 실행하는 키보드 단축키를 두지 않는다.

## 문구와 접근성

[항목 추가 화면 공통 디자인](./entity-add.md)의 `문구와 접근성`에 더해 한국어 환경과 그 외 기본 환경에서는 다음 문구를 사용한다.

| 문구 | 한국어 | 그 외 기본 |
| --- | --- | --- |
| 상단 바 제목 | `루틴 추가` | `Add Routine` |
| 추가 버튼 접근성 이름 | `루틴 추가` | `Add routine` |

제목 입력과 설명 입력의 이름과 표현은 각 입력 컴포넌트의 디자인을 따른다.
