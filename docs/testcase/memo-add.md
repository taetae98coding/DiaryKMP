# MemoAdd 테스트 케이스

기준 스펙: [MemoAdd 화면 스펙](../spec/memo-add.md)

이 문서에서 `domain > 추가 요청 제한`, `feature > 뒤로가기` 절은 [항목 추가 화면 공통 스펙](../spec/entity-add.md)이 소유한다. 나머지 케이스의 절은 기준 스펙이 소유한다.

태그 입력의 표시와 조작, 태그 관련 문구의 케이스는 [메모 태그 입력 컴포넌트 테스트 케이스](./memo-tag-input.md)에서, 웹 입력의 표시와 조작, 웹 관련 문구의 케이스는 [메모 웹 입력 컴포넌트 테스트 케이스](./memo-web-input.md)에서 다룬다. 이 문서에는 MemoAdd 화면에서만 다른 초기 상태와 추가 결과의 케이스만 둔다.

## feature

### TC-MEMO-ADD-FEATURE-005: 뒤로가기를 사용하면 이전 화면으로 돌아간다

- 근거: `feature > 뒤로가기`
- Given: MemoAdd 화면이 표시되어 있다.
- When: 사용자가 뒤로가기를 실행한다.
- Then: MemoAdd 화면이 닫히고 진입하기 전 화면이 표시된다.

### TC-MEMO-ADD-FEATURE-006: 추가에 성공하면 화면을 유지하고 다음 메모 입력 상태로 바뀐다

- 근거: `feature > 메모 추가 실행`, `feature > 추가 성공`
- Given: 메모 추가가 성공하도록 설정되어 있고, 공백이 아닌 제목과 설명이 입력되어 있으며 컬러가 선택되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: MemoAdd 화면이 그대로 유지되고 제목과 설명은 비워지며 다음 메모를 위한 무작위 컬러가 새로 제시된다.

### TC-MEMO-ADD-FEATURE-007: 추가 성공을 안내한다

- 근거: `feature > 추가 성공`
- Given: 메모 추가가 성공하도록 설정되어 있고 공백이 아닌 제목이 입력되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 메모가 추가되었음을 안내한다.

### TC-MEMO-ADD-FEATURE-008: 추가를 처리하는 동안 진행 중인 상태를 표시한다

- 근거: `feature > 메모 추가 실행`
- Given: 메모 추가 처리가 완료되지 않고 진행 중인 상태로 설정되어 있고, 공백이 아닌 제목이 입력되어 있다.
- When: 사용자가 메모 추가를 실행한 뒤 화면을 확인한다.
- Then: 추가 처리가 끝날 때까지 진행 중인 상태가 표시된다.

### TC-MEMO-ADD-FEATURE-009: 제목을 입력하지 않으면 입력 내용을 유지한다

- 근거: `feature > 제목 미입력`
- Given: MemoAdd 화면이 표시되어 있고, 제목 입력 칸이 테스트 데이터의 상태다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 메모가 추가되지 않고 입력 중이던 내용이 지워지지 않은 채 유지된다.
- 테스트 데이터:

| 제목 입력 칸 상태 |
| --- |
| 비어 있음 |
| 공백 문자만 입력됨 |

### TC-MEMO-ADD-FEATURE-010: 제목 미입력을 안내한다

- 근거: `feature > 제목 미입력`
- Given: 제목 입력 칸이 비어 있거나 공백 문자만 입력되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 제목 입력을 요청하는 안내가 표시된다.

### TC-MEMO-ADD-FEATURE-011: 화면이 처음 나타나면 기간을 사용하지 않는 상태다

- 근거: `feature > 메모 내용 입력`, `domain > 기간 선택의 시작 상태`
- Given: MemoHome이나 TagDetail 메모 탭에서 MemoAdd 화면에 진입할 수 있다.
- When: 사용자가 MemoAdd 화면에 진입한다.
- Then: 메모 기간을 사용하지 않는 상태로 표시된다.

### TC-MEMO-ADD-FEATURE-047: 캘린더 홈 화면에서 기간을 선택해 진입하면 그 기간이 종일 기간으로 입력되어 있다

- 근거: `feature > 진입`, `domain > 기간 선택의 시작 상태`
- Given: 캘린더 홈 화면에서 2026년 7월 14일부터 7월 17일까지의 기간을 선택해 MemoAdd 화면에 진입할 수 있다.
- When: 사용자가 MemoAdd 화면에 진입한다.
- Then: 시작이 2026년 7월 14일이고 종료가 2026년 7월 17일인 종일 기간을 사용하는 상태로 표시된다.

### TC-MEMO-ADD-FEATURE-013: 추가에 성공해도 선택한 기간이 유지된다

- 근거: `feature > 추가 성공`
- Given: 메모 추가가 성공하도록 설정되어 있고, 공백이 아닌 제목이 입력되어 있으며 기간을 사용하도록 설정해 기간을 선택한 상태다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 기간 사용 상태와 선택한 기간이 그대로 유지되어, 사용자는 같은 기간으로 다음 메모를 이어서 추가할 수 있다.

### TC-MEMO-ADD-FEATURE-016: MemoHome에서 진입하면 선택한 태그가 없는 상태로 시작한다

- 근거: `feature > 태그 입력`, `domain > 태그 선택의 시작 상태`
- Given: 계정과 연결된 태그가 있고 MemoHome에서 MemoAdd 화면에 진입할 수 있다.
- When: 사용자가 MemoHome의 메모 추가 행동으로 MemoAdd 화면에 진입한다.
- Then: 선택한 태그와 대표 태그가 없는 상태로 표시된다.

### TC-MEMO-ADD-FEATURE-042: TagDetail 메모 탭에서 진입하면 대상 태그를 대표 태그로 표시한다

- 근거: `feature > 진입`, `feature > 태그 입력`, `domain > 태그 선택의 시작 상태`
- Given: 완료되지 않고 삭제되지 않은 대상 태그의 TagDetail 메모 탭이 표시되어 있다.
- When: 사용자가 메모 추가 행동으로 MemoAdd 화면에 진입한다.
- Then: 대상 태그가 선택되고 대표 태그로 지정된 상태로 표시된다.

### TC-MEMO-ADD-FEATURE-028: 추가에 성공해도 선택한 태그와 대표 태그 지정이 유지된다

- 근거: `feature > 추가 성공`
- Given: 메모 추가가 성공하도록 설정되어 있고, 공백이 아닌 제목이 입력되어 있으며, 태그 두 개가 선택되고 그중 하나가 대표 태그로 지정되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 선택한 두 태그와 대표 태그 지정이 그대로 유지되어, 사용자는 같은 태그로 다음 메모를 이어서 추가할 수 있다.

### TC-MEMO-ADD-FEATURE-045: 화면 구성이 변경되어도 선택한 태그와 대표 태그 지정을 유지한다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 태그 두 개를 선택하고 그중 하나를 대표 태그로 지정한 상태다.
- When: 화면이 회전하거나 창 크기가 바뀌어 화면 구성이 변경된다.
- Then: 변경 전에 선택한 두 태그와 대표 태그 지정이 그대로 유지된다.

### TC-MEMO-ADD-FEATURE-046: 시스템이 앱을 정리한 뒤 화면을 복원하면 선택한 태그가 유지되지 않는다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 태그 두 개를 선택하고 그중 하나를 대표 태그로 지정한 상태다.
- When: 시스템이 앱을 메모리에서 정리한 뒤 사용자가 같은 경로로 화면을 복원한다.
- Then: 이전에 선택한 태그와 대표 태그 지정은 복원되지 않고 진입 경로의 초기 선택 상태로 시작한다.
- 작성하지 않는 이유: 현재 테스트 환경에서는 시스템의 앱 정리와 복원을 결정적으로 제어할 수 없다. 앱 정리와 복원을 제어하는 테스트 환경이 갖춰지면 자동화한다.

### TC-MEMO-ADD-FEATURE-031: 앱을 다시 실행한 뒤 MemoHome에서 새로 진입하면 선택한 태그가 없는 상태로 시작한다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 태그를 선택하고 대표 태그를 지정한 뒤 앱을 종료했다.
- When: 사용자가 앱을 다시 실행하고 MemoHome에서 MemoAdd 화면에 새로 진입한다.
- Then: 선택한 태그와 대표 태그가 없는 상태로 표시된다.
- 작성하지 않는 이유: 현재 테스트 환경에서는 앱 종료와 재실행을 결정적으로 제어할 수 없다. 앱 재실행을 제어하는 테스트 환경이 갖춰지면 자동화한다.

### TC-MEMO-ADD-FEATURE-043: 앱을 다시 실행한 뒤 TagDetail 메모 탭에서 새로 진입하면 대상 태그만 대표 태그로 선택된다

- 근거: `domain > 진행 상태 유지`
- Given: TagDetail 메모 탭에서 진입한 MemoAdd 화면에서 대상 태그 외의 태그를 추가로 선택하고 대표 태그를 바꾼 뒤 앱을 종료했다.
- When: 사용자가 앱을 다시 실행하고 같은 대상 태그의 TagDetail 메모 탭에서 MemoAdd 화면에 새로 진입한다.
- Then: 이전에 바꾼 선택은 복원되지 않고 메모 탭의 상세 대상 태그만 선택되어 대표 태그로 표시된다.
- 작성하지 않는 이유: 현재 테스트 환경에서는 앱 종료와 재실행을 결정적으로 제어할 수 없다. 앱 재실행을 제어하는 테스트 환경이 갖춰지면 자동화한다.

### TC-MEMO-ADD-FEATURE-055: 진입하면 선택한 웹 항목이 없는 상태로 시작한다

- 근거: `domain > 웹 선택의 시작 상태`
- Given: 계정과 연결된 웹 항목이 저장되어 있다.
- When: 사용자가 MemoAdd 화면에 진입한다.
- Then: 웹 입력에 선택한 웹 항목이 없는 상태로 표시된다.

### TC-MEMO-ADD-FEATURE-056: 추가에 성공해도 선택한 웹 항목이 유지된다

- 근거: `feature > 추가 성공`
- Given: 공백이 아닌 제목이 입력되어 있고 웹 항목이 선택되어 있다.
- When: 사용자가 메모 추가를 실행해 성공한다.
- Then: 선택한 웹 항목이 그대로 유지되어 표시된다.

### TC-MEMO-ADD-FEATURE-057: 화면 구성이 변경되어도 선택한 웹 항목을 유지한다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 웹 항목이 선택되어 있다.
- When: 화면 회전이나 창 크기 변경으로 화면 구성이 변경된다.
- Then: 선택한 웹 항목이 그대로 표시된다.

### TC-MEMO-ADD-FEATURE-058: 시스템이 앱을 정리한 뒤 화면을 복원하면 선택한 웹 항목이 유지되지 않는다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 웹 항목이 선택된 상태로 시스템이 앱을 메모리에서 정리했다.
- When: 사용자가 앱으로 돌아와 MemoAdd 화면이 복원된다.
- Then: 선택한 웹 항목이 없는 상태로 표시된다.
- 작성하지 않는 이유: 현재 테스트 환경에서는 시스템의 프로세스 정리와 복원을 결정적으로 제어할 수 없다. 프로세스 재생성을 제어하는 테스트 환경이 갖춰지면 자동화한다.

### TC-MEMO-ADD-FEATURE-048: 진입하면 선택한 장소가 없는 상태로 시작한다

- 근거: `domain > 장소 선택의 시작 상태`
- Given: 계정과 연결된 장소가 저장되어 있다.
- When: 사용자가 MemoAdd 화면에 진입한다.
- Then: 장소 카드에 선택한 장소가 없는 상태로 표시된다.

### TC-MEMO-ADD-FEATURE-049: 추가에 성공해도 선택한 장소가 유지된다

- 근거: `feature > 추가 성공`
- Given: 공백이 아닌 제목이 입력되어 있고 장소가 선택되어 있다.
- When: 사용자가 메모 추가를 실행해 성공한다.
- Then: 선택한 장소가 그대로 유지되어 표시된다.

### TC-MEMO-ADD-FEATURE-050: 화면 구성이 변경되어도 선택한 장소를 유지한다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 장소가 선택되어 있다.
- When: 화면 회전이나 창 크기 변경으로 화면 구성이 변경된다.
- Then: 선택한 장소가 그대로 표시된다.

### TC-MEMO-ADD-FEATURE-051: 시스템이 앱을 정리한 뒤 화면을 복원하면 선택한 장소가 유지되지 않는다

- 근거: `domain > 진행 상태 유지`
- Given: MemoAdd 화면에서 장소가 선택된 상태로 시스템이 앱을 메모리에서 정리했다.
- When: 사용자가 앱으로 돌아와 MemoAdd 화면이 복원된다.
- Then: 선택한 장소가 없는 상태로 표시된다.
- 작성하지 않는 이유: 현재 테스트 환경에서는 시스템의 프로세스 정리와 복원을 결정적으로 제어할 수 없다. 프로세스 재생성을 제어하는 테스트 환경이 갖춰지면 자동화한다.

### TC-MEMO-ADD-FEATURE-052: 화면에 처음 진입하면 제목 입력에 초점이 있다

- 근거: `feature > 메모 내용 입력`
- Given: 사용자가 메모 추가를 선택했다.
- When: MemoAdd 화면이 처음 표시된다.
- Then: 입력 초점이 제목 입력에 있다.

### TC-MEMO-ADD-FEATURE-053: 추가에 성공하면 제목 입력으로 초점을 옮긴다

- 근거: `feature > 추가 성공`
- Given: 메모 추가가 성공하도록 설정되어 있고, 공백이 아닌 제목이 입력되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 입력 초점이 제목 입력으로 옮겨진다.

### TC-MEMO-ADD-FEATURE-054: 제목 미입력으로 추가하면 제목 입력으로 초점을 옮긴다

- 근거: `feature > 제목 미입력`
- Given: 제목이 비어 있고 설명을 입력해 설명 입력에 초점이 있는 MemoAdd 화면이 표시되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 입력 초점이 제목 입력으로 옮겨진다.

## domain

### TC-MEMO-ADD-DOMAIN-001: 제목이 없으면 메모를 추가하지 않는다

- 근거: `domain > 메모 추가 성립 조건`
- Given: 메모를 추가할 수 있는 상태다.
- When: 제목이 테스트 데이터인 상태로 메모 추가를 실행한다.
- Then: 메모 추가가 성립하지 않고 새 메모가 저장되지 않는다.
- 테스트 데이터:

| 제목 |
| --- |
| 빈 문자열 |
| 공백 문자로만 이루어진 값 |

### TC-MEMO-ADD-DOMAIN-002: 추가를 처리하는 동안 전달된 추가 요청은 처리하지 않는다

- 근거: `domain > 추가 요청 제한`
- Given: 앞선 메모 추가가 아직 처리 중이고, 공백이 아닌 제목이 입력되어 있다.
- When: 같은 화면에서 메모 추가 요청이 한 번 더 전달된다.
- Then: 추가 요청이 한 번만 처리되어 같은 입력으로 메모가 중복 저장되지 않는다.

### TC-MEMO-ADD-DOMAIN-006: 태그를 선택하지 않아도 메모를 추가할 수 있다

- 근거: `domain > 태그 선택과 메모 추가 성립 조건`, `domain > 메모 추가 성립 조건`
- Given: 공백이 아닌 제목이 입력되어 있고 선택한 태그와 대표 태그 지정이 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 메모 추가가 성립해 새 메모가 저장된다.

### TC-MEMO-ADD-DOMAIN-007: 선택한 태그가 완료되거나 삭제되면 선택에서 제외된 것으로 표시된다

- 근거: `domain > 태그 선택과 메모 추가 성립 조건`
- Given: MemoAdd 화면에서 한 태그가 선택되고 대표 태그로 지정된 상태다.
- When: 그 태그가 다른 경로로 테스트 데이터의 상태로 바뀐다.
- Then: 태그 입력에서 그 태그가 선택에서 제외되고 대표 태그 지정도 해제된 것으로 표시된다.
- 테스트 데이터:

| 태그 상태 변경 |
| --- |
| 완료 |
| 삭제 |

### TC-MEMO-ADD-DOMAIN-012: 웹 항목을 선택하지 않아도 메모를 추가할 수 있다

- 근거: `domain > 웹 선택과 메모 추가 성립 조건`, `domain > 메모 추가 성립 조건`
- Given: 공백이 아닌 제목이 입력되어 있고 선택한 웹 항목이 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 메모 추가가 성립해 새 메모가 저장된다.

### TC-MEMO-ADD-DOMAIN-011: 선택한 웹 항목이 삭제되면 선택에서 제외된 것으로 표시된다

- 근거: `domain > 웹 선택과 메모 추가 성립 조건`
- Given: MemoAdd 화면에서 한 웹 항목이 선택된 상태다.
- When: 그 웹 항목이 다른 경로로 삭제된 상태로 바뀐다.
- Then: 웹 입력에서 그 웹 항목이 선택에서 제외된 것으로 표시된다.

### TC-MEMO-ADD-DOMAIN-008: 장소를 선택하지 않아도 메모를 추가할 수 있다

- 근거: `domain > 장소 선택과 메모 추가 성립 조건`, `domain > 메모 추가 성립 조건`
- Given: 공백이 아닌 제목이 입력되어 있고 선택한 장소가 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 메모 추가가 성립해 새 메모가 저장된다.

### TC-MEMO-ADD-DOMAIN-009: 선택한 장소가 삭제되면 선택에서 제외된 것으로 표시된다

- 근거: `domain > 장소 선택과 메모 추가 성립 조건`
- Given: MemoAdd 화면에서 한 장소가 선택된 상태다.
- When: 그 장소가 다른 경로로 삭제된 상태로 바뀐다.
- Then: 장소 카드에서 그 장소가 선택에서 제외된 것으로 표시된다.

### TC-MEMO-ADD-DOMAIN-010: 현재 계정을 확인할 수 없으면 메모를 추가하지 않는다

- 근거: `domain > 항목의 계정 연결`
- Given: 현재 계정 조회가 실패하도록 설정되어 있다.
- When: 공백이 아닌 제목으로 메모 추가를 실행한다.
- Then: 메모가 기록되지 않고 계정 조회의 실패가 그대로 전달된다.

## data

### TC-MEMO-ADD-DATA-001: 유효한 제목으로 추가하면 현재 계정과 연결된 메모로 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 초기 상태`, `domain > 메모의 연결`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목으로 메모 추가를 실행할 수 있다.
- When: 사용자가 해당 제목으로 메모 추가를 실행한다.
- Then: 새 메모 한 건이 현재 계정과 연결되어 저장되며, 저장된 메모는 다른 메모와 겹치지 않는 고유 식별자를 가지고 완료되지 않은 상태이자 삭제되지 않은 상태이며, 생성 시각과 수정 시각이 서로 같다.

### TC-MEMO-ADD-DATA-002: 입력한 설명과 컬러가 저장된 메모에 함께 포함된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 초기 상태`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목과 입력한 설명, 선택한 컬러로 메모 추가를 실행할 수 있다.
- When: 사용자가 해당 제목, 설명, 컬러로 메모 추가를 실행한다.
- Then: 저장된 메모에 입력한 설명과 선택한 컬러가 그대로 포함된다.

### TC-MEMO-ADD-DATA-003: 선택한 기간이 저장된 메모에 함께 포함된다

- 근거: `data > 메모 생성과 저장`, `domain > 메모의 기간`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 기간을 사용하도록 설정해 테스트 데이터의 기간을 선택한 상태다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모에 선택한 기간이 종일 여부와 함께 그대로 포함된다.
- 테스트 데이터:

| 기간 형태 | 예시 |
| --- | --- |
| 종일 기간 | `2026-07-20` ~ `2026-07-21` |
| 날짜·시간 기간 | `2026-07-20 13:30` ~ `2026-07-21 14:00` |

### TC-MEMO-ADD-DATA-004: 기간을 사용하지 않는 상태로 추가하면 기간 없는 메모로 저장된다

- 근거: `domain > 메모의 기간`, `data > 메모 생성과 저장`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 기간을 사용하지 않는 상태다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모에 기간이 포함되지 않는다.

### TC-MEMO-ADD-DATA-005: 선택한 태그가 메모와 함께 연결되어 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 태그`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 계정과 연결된 태그 두 개가 선택되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 새 메모가 저장되고 선택한 두 태그가 그 메모에 연결된 태그로 함께 저장된다.

### TC-MEMO-ADD-DATA-006: 대표 태그로 지정한 태그가 메모의 대표 태그로 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 태그`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 태그 두 개가 선택되고 그중 하나가 대표 태그로 지정되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모의 대표 태그가 지정한 그 태그이고, 선택한 두 태그가 모두 그 메모에 연결된 태그로 저장된다.

### TC-MEMO-ADD-DATA-007: 태그를 선택하지 않으면 태그 연결과 대표 태그 없이 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 태그`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 선택한 태그와 대표 태그 지정이 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모에 연결된 태그가 없고 대표 태그도 지정되지 않은 상태로 저장된다.

### TC-MEMO-ADD-DATA-008: 대표 태그만 지정하고 추가해도 그 태그가 연결로 함께 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 태그`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 태그 하나가 대표 태그로 지정되어 있고 그 태그 외에 선택한 태그가 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모의 대표 태그가 그 태그이고, 같은 태그가 그 메모에 연결된 태그로도 저장된다.

### TC-MEMO-ADD-DATA-009: 선택한 장소가 메모와 함께 연결되어 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 장소`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 계정과 연결된 장소 두 개가 선택되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 새 메모가 저장되고 선택한 두 장소가 그 메모에 연결된 장소로 함께 저장된다.

### TC-MEMO-ADD-DATA-010: 장소를 선택하지 않으면 장소 연결 없이 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 장소`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 선택한 장소가 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모에 연결된 장소가 없는 상태로 저장된다.

### TC-MEMO-ADD-DATA-011: 장소를 선택하는 것만으로는 저장된 메모가 바뀌지 않는다

- 근거: `feature > 장소 표시와 선택`
- Given: 로그인한 계정에 저장된 메모가 있고, MemoAdd 화면이 표시되어 있다.
- When: 사용자가 장소 선택 목록에서 장소를 선택한다.
- Then: 메모 추가를 실행하기 전에는 어떤 메모에도 장소 연결이 저장되지 않는다.

### TC-MEMO-ADD-DATA-012: 태그 선택 목록을 확인한 범위는 연결되는 태그에 영향을 주지 않는다

- 근거: `data > 태그 목록 조회`
- Given: 로그인한 계정에 선택할 수 있는 태그가 여러 페이지 분량으로 저장되어 있고, TagDetail 메모 탭에서 진입해 정렬 순서 뒤쪽에 있는 대상 태그가 선택되고 대표 태그로 지정되어 있으며, 사용자는 태그 선택 목록을 열지 않았다.
- When: 사용자가 공백이 아닌 제목을 입력하고 메모 추가를 실행한다.
- Then: 그 대상 태그가 메모와 연결되고 메모의 대표 태그로 저장된다.

### TC-MEMO-ADD-DATA-013: 선택한 뒤 삭제된 장소도 메모에 연결된다

- 근거: `domain > 추가되는 메모의 장소`, `data > 장소 목록 조회`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 선택한 장소 두 개 중 하나가 선택 이후 삭제된 상태로 바뀌었다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 선택한 두 장소가 모두 새 메모에 연결되어 저장된다.

### TC-MEMO-ADD-DATA-014: 선택한 뒤 완료되거나 삭제된 태그도 메모에 연결된다

- 근거: `domain > 추가되는 메모의 태그`, `data > 태그 목록 조회`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 선택한 태그 두 개 중 대표 태그로 지정한 태그가 선택 이후 완료되거나 삭제된 상태로 바뀌었다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 선택한 두 태그가 모두 새 메모에 연결되고, 대표 태그 지정도 그대로 저장된다.

### TC-MEMO-ADD-DATA-015: 선택한 웹 항목이 메모와 함께 연결되어 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 웹 항목`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 계정과 연결된 웹 항목 두 개가 선택되어 있다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 새 메모가 저장되고 선택한 두 웹 항목이 그 메모에 연결된 웹 항목으로 함께 저장된다.

### TC-MEMO-ADD-DATA-016: 웹 항목을 선택하지 않으면 웹 연결 없이 저장된다

- 근거: `data > 메모 생성과 저장`, `domain > 추가되는 메모의 웹 항목`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 선택한 웹 항목이 없다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 저장된 메모에 연결된 웹 항목이 없는 상태로 저장된다.

### TC-MEMO-ADD-DATA-017: 웹 항목을 선택하는 것만으로는 저장된 메모가 바뀌지 않는다

- 근거: `feature > 웹 입력`
- Given: 로그인한 계정에 저장된 메모가 있고, MemoAdd 화면이 표시되어 있다.
- When: 사용자가 웹 선택 목록에서 웹 항목을 선택한다.
- Then: 메모 추가를 실행하기 전에는 어떤 메모에도 웹 연결이 저장되지 않는다.

### TC-MEMO-ADD-DATA-018: 선택한 뒤 삭제된 웹 항목도 메모에 연결된다

- 근거: `domain > 추가되는 메모의 웹 항목`, `data > 웹 목록 조회`
- Given: 로그인한 계정이 있고, 공백이 아닌 제목이 입력되어 있으며, 선택한 웹 항목 두 개 중 하나가 선택 이후 삭제된 상태로 바뀌었다.
- When: 사용자가 메모 추가를 실행한다.
- Then: 선택한 두 웹 항목이 모두 새 메모에 연결되어 저장된다.
