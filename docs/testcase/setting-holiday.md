# SettingHoliday 테스트 케이스

기준 스펙: [SettingHoliday 화면 스펙](../spec/client/setting-holiday.md)

설정 화면 본문에서 `공휴일` 항목을 선택해 SettingHoliday로 이동하는 케이스는 [SettingHome 테스트 케이스](./setting-home.md)에서 다룬다. 설정 목록과 함께 표시되는 상태의 시스템 뒤로가기 결과는 [Setting 목록·상세 배치 테스트 케이스](./setting-list-detail.md)에서, 선택한 항목이 캘린더 공휴일 조회에 적용되는 결과는 [공휴일 노출 설정 테스트 케이스](./holiday-visibility.md)에서 다룬다. 국가 설정의 저장과 적용 국가 판정은 [공휴일 국가 설정 테스트 케이스](./holiday-country.md)에서 다룬다.

## feature

### TC-SETTING-HOLIDAY-FEATURE-003: 단독 화면에서 뒤로가면 설정 목록으로 돌아간다

- 근거: `feature > 뒤로가기`
- Given: SettingHoliday 화면이 단독으로 표시되어 뒤로가기 동작이 제공되고 있다.
- When: 사용자가 뒤로가기 동작을 선택한다.
- Then: 이전 화면으로 돌아가는 요청이 한 번 발생한다.

이 요청으로 설정 목록이 다시 표시되는 결과는 [Setting 목록·상세 배치 테스트 케이스](./setting-list-detail.md)의 TC-SETTING-LIST-DETAIL-FEATURE-013이, 그동안 공통 내비게이션을 계속 제공하지 않는 결과는 [TopLevelNavigation 테스트 케이스](./top-level-navigation.md)의 TC-TOP-LEVEL-NAVIGATION-FEATURE-004가 다룬다.

### TC-SETTING-HOLIDAY-FEATURE-005: 동작 버튼을 펼치면 일괄 선택 동작을 제공한다

- 근거: `feature > 일괄 선택`
- Given: 저장된 공휴일과 노출 설정을 확인했고 검색어가 비어 있다.
- When: 사용자가 일괄 선택 동작 버튼을 선택한다.
- Then: 전체 선택, 전체 해제, 쉬는 날만 선택하는 동작이 제공된다.

### TC-SETTING-HOLIDAY-FEATURE-006: 저장된 공휴일 항목과 선택 상태를 표시한다

- 근거: `feature > 공휴일 선택`
- Given: 저장된 공휴일은 서로 다른 공휴일 이름 세 개로 구성되고 그중 두 이름이 선택되어 있다.
- When: 사용자가 SettingHoliday 화면 본문을 확인한다.
- Then: 세 공휴일 항목이 각각의 선택 상태와 함께 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-007: 쉬는 날인 항목만 쉬는 날로 표시한다

- 근거: `feature > 공휴일 선택`
- Given: 쉬는 날인 공휴일과 쉬는 날이 아닌 공휴일이 하나씩 저장되어 있다.
- When: 사용자가 두 공휴일 항목을 확인한다.
- Then: 쉬는 날인 항목만 쉬는 날로 구분되고 쉬는 날이 아닌 항목은 그렇게 구분되지 않는다.

### TC-SETTING-HOLIDAY-FEATURE-008: 공휴일 항목을 선택하면 선택 상태가 바뀐다

- 근거: `feature > 공휴일 선택`, `domain > 선택 상태`
- Given: 저장된 공휴일과 노출 설정이 확인되어 있고 설정 변경이 성공하는 상태다.
- When: 사용자가 테스트 데이터의 공휴일 항목을 선택한다.
- Then: 해당 공휴일 이름의 목표 상태가 한 번 저장되고, 저장된 결과가 반영되면 해당 항목이 목표 상태로 바뀐다.
- 테스트 데이터:

| 기존 상태 | 목표 상태 |
| --- | --- |
| 선택 | 선택 해제 |
| 선택 해제 | 선택 |

### TC-SETTING-HOLIDAY-FEATURE-009: 전체 선택은 모든 항목을 선택한다

- 근거: `feature > 일괄 선택`, `domain > 일괄 선택 기준`
- Given: 현재 공휴일 중 일부만 선택되어 있고, 현재 공휴일에 없는 이름도 숨김으로 저장되어 있으며 설정 변경이 성공하는 상태다.
- When: 사용자가 `전체 선택`을 선택한다.
- Then: 숨긴 공휴일 이름이 없는 목록이 한 번 저장되어 현재 공휴일에 없는 기존 이름도 빠지고, 저장된 결과가 반영되면 모든 공휴일 항목이 선택 상태로 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-010: 전체 해제는 모든 항목을 선택 해제한다

- 근거: `feature > 일괄 선택`, `domain > 일괄 선택 기준`
- Given: 현재 공휴일 중 일부가 선택되어 있고, 현재 공휴일에 없는 이름도 숨김으로 저장되어 있으며 설정 변경이 성공하는 상태다.
- When: 사용자가 `전체 해제`를 선택한다.
- Then: 현재 전체 공휴일의 모든 이름만 담은 숨긴 공휴일 이름 목록이 한 번 저장되어 현재 공휴일에 없는 기존 이름은 빠지고, 저장된 결과가 반영되면 모든 공휴일 항목이 선택 해제 상태로 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-011: 쉬는 날만 선택은 쉬는 날 여부에 맞춰 선택한다

- 근거: `feature > 일괄 선택`, `domain > 일괄 선택 기준`
- Given: 쉬는 날인 공휴일 이름과 쉬는 날이 아닌 공휴일 이름이 각각 있고 현재 선택 상태가 쉬는 날 여부와 다르며, 현재 공휴일에 없는 이름도 숨김으로 저장되어 있고 설정 변경이 성공하는 상태다.
- When: 사용자가 `쉬는 날만 선택`을 선택한다.
- Then: 현재 전체 공휴일 중 쉬는 날이 아닌 이름만 담은 숨긴 공휴일 이름 목록이 한 번 저장되어 현재 공휴일에 없는 기존 이름은 빠지고, 저장된 결과가 반영되면 쉬는 날인 모든 항목은 선택되고 쉬는 날이 아닌 모든 항목은 선택 해제된다.

### TC-SETTING-HOLIDAY-FEATURE-012: 일괄 선택의 목표 상태와 같으면 새 저장값을 만들지 않는다

- 근거: `feature > 일괄 선택`, `domain > 일괄 선택 기준`
- Given: 현재 저장된 숨긴 공휴일 이름 목록이 테스트 데이터의 전체 교체 결과와 이미 정확히 같다.
- When: 사용자가 테스트 데이터의 일괄 선택을 선택한다.
- Then: 선택 상태가 그대로 유지되고 새 저장값이나 화면 상태 갱신이 생기지 않는다.
- 테스트 데이터:

| 저장된 숨긴 공휴일 이름 목록 | 일괄 선택 |
| --- | --- |
| 빈 목록 | `전체 선택` |
| 현재 전체 공휴일의 모든 이름 | `전체 해제` |
| 현재 전체 공휴일 중 쉬는 날이 아닌 이름 | `쉬는 날만 선택` |

### TC-SETTING-HOLIDAY-FEATURE-013: 공휴일을 확인하는 동안 확인 중 상태를 제공한다

- 근거: `feature > 확인 중`
- Given: 저장된 공휴일과 노출 설정을 아직 확인하고 있다.
- When: 사용자가 SettingHoliday 화면 본문을 확인한다.
- Then: 확인 중 상태가 제공되고 선택할 수 있는 공휴일 항목이 없다.

### TC-SETTING-HOLIDAY-FEATURE-018: 원본 데이터가 바뀌면 목록을 갱신한다

- 근거: `feature > 상태 갱신과 유지`
- Given: SettingHoliday 화면에서 저장된 공휴일과 선택 상태를 확인하고 있다.
- When: 저장된 공휴일 또는 노출 설정이 바뀐다.
- Then: 사용자의 별도 조작 없이 공휴일 목록과 각 항목의 선택 상태가 최신 값으로 바뀐다.

### TC-SETTING-HOLIDAY-FEATURE-020: 목록을 스크롤해도 일괄 선택 동작을 계속 사용할 수 있다

- 근거: `feature > 일괄 선택`
- Given: 한 화면에 다 보이지 않을 만큼 많은 공휴일이 저장되어 있다.
- When: 사용자가 공휴일 목록을 마지막 항목까지 스크롤한 뒤 일괄 선택 동작 버튼을 선택한다.
- Then: 동작 버튼이 계속 표시되어 있고 전체 선택, 전체 해제, 쉬는 날만 선택하는 동작이 제공된다.

### TC-SETTING-HOLIDAY-FEATURE-021: 검색어를 입력하면 일치하는 항목만 표시한다

- 근거: `feature > 검색`
- Given: 검색어와 일치하는 공휴일과 일치하지 않는 공휴일이 함께 저장되어 있고 목록이 표시되어 있다.
- When: 사용자가 검색어를 입력한다.
- Then: 일치하는 항목만 목록에 남고, 남은 항목의 순서와 선택 상태는 전체 목록에서와 같다.

### TC-SETTING-HOLIDAY-FEATURE-022: 검색 결과가 없으면 빈 상태 안내를 표시한다

- 근거: `feature > 검색`
- Given: 공휴일 목록이 표시되어 있다.
- When: 사용자가 어떤 공휴일과도 일치하지 않는 검색어를 입력한다.
- Then: 결과가 없음을 알리는 빈 상태가 표시되고, 검색어를 계속 수정하거나 지울 수 있다.

### TC-SETTING-HOLIDAY-FEATURE-023: 검색어를 모두 지우면 전체 목록으로 돌아간다

- 근거: `feature > 검색`
- Given: 검색어로 목록이 필터링되어 있다.
- When: 사용자가 검색어를 모두 지운다.
- Then: 저장된 모든 공휴일 항목이 다시 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-024: 일괄 선택은 검색어가 비어 있을 때만 제공한다

- 근거: `feature > 일괄 선택`
- Given: 공휴일 목록과 일괄 선택 동작 버튼이 표시되어 있다.
- When: 사용자가 검색어를 입력한 뒤 다시 모두 지운다.
- Then: 검색어가 있는 동안 일괄 선택 동작 버튼이 제공되지 않고, 검색어를 모두 지우면 다시 제공된다.

### TC-SETTING-HOLIDAY-FEATURE-025: 필터링된 목록에서도 선택 상태를 바꿀 수 있다

- 근거: `feature > 검색`, `domain > 선택 상태`
- Given: 검색어로 목록이 필터링되어 있고 설정 변경이 성공하는 상태다.
- When: 사용자가 필터링된 목록의 공휴일 항목을 선택한다.
- Then: 해당 공휴일 이름의 목표 상태가 한 번 저장되고, 저장된 결과가 반영되면 해당 항목이 목표 상태로 바뀐다.

### TC-SETTING-HOLIDAY-FEATURE-026: 화면에 진입해도 검색어 입력에 초점을 두지 않는다

- 근거: `feature > 검색`
- Given: 사용자가 설정의 `공휴일` 항목을 선택했다.
- When: SettingHoliday 화면이 표시된다.
- Then: 검색어 입력에 초점이 없다.

### TC-SETTING-HOLIDAY-FEATURE-030: 일괄 선택 동작을 고르면 펼친 동작을 접는다

- 근거: `feature > 일괄 선택`
- Given: 일괄 선택 동작 버튼을 선택해 세 동작이 펼쳐져 있고 설정 변경이 성공하는 상태다.
- When: 사용자가 펼쳐진 동작 중 하나를 고른다.
- Then: 세 동작이 더 이상 표시되지 않고 동작 버튼만 남는다.

### TC-SETTING-HOLIDAY-FEATURE-031: 동작 버튼을 다시 선택하면 아무 동작도 실행하지 않고 접는다

- 근거: `feature > 일괄 선택`
- Given: 일괄 선택 동작 버튼을 선택해 세 동작이 펼쳐져 있다.
- When: 사용자가 동작 버튼을 다시 선택한다.
- Then: 세 동작이 더 이상 표시되지 않고 선택 상태는 그대로 유지되며 새 저장값이 생기지 않는다.

### TC-SETTING-HOLIDAY-FEATURE-032: 확인 중에도 일괄 선택 동작을 펼쳐 실행할 수 있다

- 근거: `feature > 확인 중`, `domain > 일괄 선택 기준`
- Given: 저장된 공휴일과 노출 설정을 아직 확인하고 있다.
- When: 사용자가 일괄 선택 동작 버튼을 선택하고 펼쳐진 동작 중 하나를 고른다.
- Then: 세 동작이 모두 제공되고, 고른 동작이 실행된다.

### TC-SETTING-HOLIDAY-FEATURE-034: 국가 선택지를 정해진 순서로 선택 상태와 함께 표시한다

- 근거: `feature > 국가 선택`
- Given: 국가 설정에서 `기기값`과 `미국`을 고른 상태이고 확인이 끝났다.
- When: 사용자가 검색어 없이 SettingHoliday 화면 본문을 확인한다.
- Then: `기기값`, `한국`, `미국` 선택지가 이 순서로 공휴일 항목보다 먼저 표시되고, `기기값`과 `미국`만 선택 상태로 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-035: 국가 선택지를 누르면 그 선택지의 변경을 요청한다

- 근거: `feature > 국가 선택`
- Given: 국가 설정이 확인되어 있다.
- When: 사용자가 테스트 데이터의 국가 선택지를 누른다.
- Then: 해당 선택지의 변경이 한 번 요청된다.
- 테스트 데이터:

  | 누르는 선택지 |
  | --- |
  | `기기값` |
  | `한국` |
  | `미국` |

### TC-SETTING-HOLIDAY-FEATURE-036: 기기값 선택지에 기기 지역에 따라 적용되는 국가를 보여 준다

- 근거: `feature > 국가 선택`
- Given: 기기 지역이 테스트 데이터의 지역이다.
- When: 사용자가 `기기값` 선택지를 확인한다.
- Then: 선택지에 테스트 데이터의 문구가 함께 표시된다.
- 테스트 데이터:

  | 기기 지역 | 함께 표시되는 문구 |
  | --- | --- |
  | 한국 | 한국 |
  | 미국 | 미국 |
  | 일본 | 지원하지 않는 지역 |

### TC-SETTING-HOLIDAY-FEATURE-037: 검색어가 있는 동안에는 국가 선택지를 제공하지 않는다

- 근거: `feature > 국가 선택`
- Given: 국가 설정과 공휴일이 확인되어 있다.
- When: 사용자가 공휴일 이름과 일치하는 검색어를 입력했다가 모두 지운다.
- Then: 검색어가 있는 동안에는 국가 선택지가 표시되지 않고 일치하는 공휴일 항목만 표시되며, 검색어를 지우면 국가 선택지가 다시 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-038: 국가 설정이 바뀌면 공휴일 목록을 적용 국가로 다시 구성한다

- 근거: `feature > 국가 선택`, `feature > 상태 갱신과 유지`
- Given: 한국과 미국 공휴일이 저장되어 있고 적용 국가가 한국인 상태로 화면이 표시되어 있다.
- When: 국가 설정에 `미국`이 더해진다.
- Then: 별도 조작 없이 한국과 미국 공휴일 항목이 함께 표시되고, 국가 선택지의 선택 상태도 바뀐 설정으로 표시된다.

### TC-SETTING-HOLIDAY-FEATURE-039: 검색어를 지워 동작 버튼이 다시 나타나면 접힌 상태다

- 근거: `feature > 일괄 선택`
- Given: 검색어가 비어 있고 일괄 선택 동작 버튼을 선택해 세 동작이 펼쳐져 있다.
- When: 사용자가 공휴일 이름과 일치하는 검색어를 입력했다가 모두 지운다.
- Then: 동작 버튼이 다시 표시되고 세 동작은 펼쳐져 있지 않다.

### TC-SETTING-HOLIDAY-FEATURE-040: 검색어가 비어 있고 공휴일 항목이 없으면 빈 상태 없이 국가 선택지만 제공한다

- 근거: `feature > 검색`
- Given: 확인이 끝났고 적용 국가의 공휴일 항목이 하나도 없다.
- When: 사용자가 검색어 없이 SettingHoliday 화면 본문을 확인한다.
- Then: `기기값`, `한국`, `미국` 국가 선택지가 표시되고, 검색 결과가 없다는 빈 상태 안내는 표시되지 않는다.

### TC-SETTING-HOLIDAY-FEATURE-041: 화면이 재생성되면 펼쳐 둔 일괄 선택이 접힌다

- 근거: `feature > 상태 갱신과 유지`
- Given: 일괄 선택 동작 버튼을 선택해 세 동작이 펼쳐져 있다.
- When: 화면이 재생성된다.
- Then: 동작 버튼은 표시되고 세 동작은 펼쳐져 있지 않다.

### TC-SETTING-HOLIDAY-FEATURE-042: 설정이나 공휴일을 읽지 못하면 확인 중 상태를 유지한다

- 근거: `feature > 확인 중`
- Given: 테스트 데이터의 대상 읽기가 실패하도록 제어되어 있다.
- When: 사용자가 SettingHoliday 화면 본문을 확인한다.
- Then: 확인 중 상태가 계속 제공되고 별도 오류 상태로 바뀌지 않는다.
- 테스트 데이터:

| 읽기에 실패하는 대상 |
| --- |
| 국가 설정 |
| 저장된 공휴일과 노출 설정 |

### TC-SETTING-HOLIDAY-FEATURE-043: 화면이 재생성되어도 검색어와 필터링된 목록이 유지된다

- 근거: `domain > 검색어 유지`
- Given: 검색어로 목록이 필터링되어 있다.
- When: 화면이 재생성된다.
- Then: 같은 검색어가 입력되어 있고 그 검색어와 일치하는 항목만 목록에 남는다.

### TC-SETTING-HOLIDAY-FEATURE-044: 검색어는 입력을 멈출 때까지 기다리지 않고 곧바로 목록에 반영된다

- 근거: `feature > 검색`
- Given: 검색어와 일치하는 공휴일과 일치하지 않는 공휴일이 함께 저장되어 있고 목록이 표시되어 있다.
- When: 사용자가 검색어를 입력하고 입력을 멈추기를 기다리는 시간이 지나기 전에 목록을 확인한다.
- Then: 일치하는 항목만 목록에 남는다.

### TC-SETTING-HOLIDAY-FEATURE-045: 화면에 들어와도 공휴일을 새로 받아 오지 않는다

- 근거: `feature > 상태 갱신과 유지`
- Given: 적용 국가의 공휴일이 기기에 저장되어 있다.
- When: 사용자가 SettingHoliday 화면에 들어온다.
- Then: 공휴일 동기화가 요청되지 않고, 기기에 저장된 공휴일로 목록이 구성된다.
- 작성하지 않는 이유: 이 화면은 공휴일을 받아 오는 기능과 이어져 있지 않아, 화면 안에서는 받아 오기 요청이 없다는 것을 관찰할 외부 경계가 없다. 화면 진입과 앱 전체의 공휴일 동기화 요청을 함께 관찰할 수 있는 통합 테스트 환경이 갖춰지면 작성한다. 기기에 저장된 공휴일로 목록이 구성되는 것은 `TC-SETTING-HOLIDAY-FEATURE-006`이 확인한다.

### TC-SETTING-HOLIDAY-FEATURE-046: 기기 지역을 바꾸고 5초가 지나기 전에 돌아오면 이전 지역 기준을 그대로 보여 준다

- 근거: `feature > 국가 선택`
- Given: `기기값`만 고른 상태에서 기기 지역이 한국인 채로 SettingHoliday 화면이 표시되어 한국 공휴일 목록을 보여 주고 있고, 기기 지역이 미국이면 미국 공휴일로 목록이 구성된다.
- When: 사용자가 다른 앱에서 기기 지역을 미국으로 바꾸고 5초가 지나기 전에 SettingHoliday 화면으로 돌아온다.
- Then: `기기값` 선택지의 국가는 한국으로 남고, 공휴일 목록도 한국 공휴일 목록 그대로다.

### TC-SETTING-HOLIDAY-FEATURE-047: 기기 지역을 바꾸고 5초가 지난 뒤 돌아오면 바뀐 지역으로 다시 표시한다

- 근거: `feature > 국가 선택`
- Given: `기기값`만 고른 상태에서 기기 지역이 한국인 채로 SettingHoliday 화면이 표시되어 한국 공휴일 목록을 보여 주고 있고, 기기 지역이 미국이면 미국 공휴일로 목록이 구성된다.
- When: 사용자가 다른 앱에서 기기 지역을 미국으로 바꾸고 5초가 지난 뒤 SettingHoliday 화면으로 돌아온다.
- Then: `기기값` 선택지의 국가가 미국으로 바뀌고, 공휴일 목록이 미국 공휴일 목록으로 다시 구성된다.

### 작성하지 않는 이유

- 선택 상태가 화면 재생성 뒤에도 유지되는 규칙은 재생성된 화면이 기기에 저장된 선택 상태를 다시 조회하는 것과 같으므로 저장된 선택 상태를 표시하는 `TC-SETTING-HOLIDAY-FEATURE-006`과 [공휴일 노출 설정 테스트 케이스](./holiday-visibility.md)의 TC-HOLIDAY-VISIBILITY-DATA-001, TC-HOLIDAY-VISIBILITY-DATA-002로 확인하고 별도 케이스로 중복 작성하지 않는다. 백그라운드 복귀, 화면 재진입과 앱 재실행 뒤에도 유지되는 규칙은 실제 저장 영역과 실행 경계를 다시 열어야 관찰할 수 있으므로 단위 테스트 케이스로 작성하지 않는다. 자동화하려면 저장 영역을 임시 폴더에 만들고 앱 프로세스를 다시 띄워 같은 영역을 여는 기기 테스트 환경이 필요하다.
- 검색어가 백그라운드 복귀에 유지되고 재진입 시 비워지는 규칙은 설정 화면 전체의 이동 이력과 앱의 백그라운드 전환을 실제로 재현해야 관찰할 수 있으므로 단위 테스트 케이스로 작성하지 않는다. 화면 재생성에 유지되는 규칙은 `TC-SETTING-HOLIDAY-FEATURE-043`이 다룬다. 자동화하려면 설정 목록·상세 이동 이력과 앱 수명 주기를 함께 구동하는 기기 테스트 환경이 필요하다.
- `TC-SETTING-HOLIDAY-FEATURE-027`, `TC-SETTING-HOLIDAY-FEATURE-028`, `TC-SETTING-HOLIDAY-FEATURE-029`, `TC-SETTING-HOLIDAY-FEATURE-033`은 검색을 시작하고 끝내는 상태가 스펙에서 사라져 검증할 행동이 없어졌다. 남은 케이스의 ID를 옮기지 않으려고 번호는 비워 둔다.

## domain

### TC-SETTING-HOLIDAY-DOMAIN-005: 같은 이름의 공휴일 중 하나라도 쉬는 날이면 쉬는 날 항목이다

- 근거: `domain > 공휴일 항목 구성`
- Given: 같은 이름의 공휴일 중 쉬는 날인 공휴일과 쉬는 날이 아닌 공휴일이 함께 저장되어 있다.
- When: SettingHoliday에 표시할 공휴일 항목을 구성한다.
- Then: 해당 공휴일 이름은 쉬는 날인 항목으로 제공된다.

### TC-SETTING-HOLIDAY-DOMAIN-006: 표시 이름 순으로 항목을 정렬한다

- 근거: `domain > 공휴일 항목 구성`
- Given: 표시 이름이 서로 다른 공휴일들이 순서 없이 저장되어 있다.
- When: SettingHoliday에 표시할 공휴일 항목을 구성한다.
- Then: 표시 이름 오름차순으로 정렬된다.

### TC-SETTING-HOLIDAY-DOMAIN-007: 숨긴 공휴일 이름만 선택 해제 상태로 제공한다

- 근거: `domain > 선택 상태`
- Given: 서로 다른 이름의 공휴일들이 저장되어 있고 그중 일부 이름만 숨김으로 저장되어 있다.
- When: SettingHoliday에 표시할 공휴일 항목을 구성한다.
- Then: 숨김으로 저장된 이름만 선택 해제 상태이고 나머지 이름은 선택 상태다.

### TC-SETTING-HOLIDAY-DOMAIN-008: 공백을 무시한 부분 일치로 검색을 판정한다

- 근거: `domain > 검색 일치`
- Given: 표시 이름이 `대체 공휴일(설날)`인 항목이 있다.
- When: 테스트 데이터의 검색어로 일치를 판정한다.
- Then: 테스트 데이터의 일치 여부대로 판정된다.
- 테스트 데이터:

| 검색어 | 일치 여부 |
| --- | --- |
| `설날` | 일치 |
| `대체공휴일` | 일치 |
| ` 대 체 ` | 일치 |
| `추석` | 불일치 |

### TC-SETTING-HOLIDAY-DOMAIN-009: 영문 검색어는 대소문자를 구분하지 않는다

- 근거: `domain > 검색 일치`
- Given: 영문이 포함된 표시 이름을 가진 항목이 있다.
- When: 같은 글자의 대소문자만 다른 검색어로 일치를 판정한다.
- Then: 대소문자와 관계없이 일치로 판정된다.

### TC-SETTING-HOLIDAY-DOMAIN-011: 공백뿐인 검색어는 검색어가 없는 것으로 본다

- 근거: `domain > 검색 일치`
- Given: 여러 공휴일 항목이 있다.
- When: 공백 문자로만 구성된 검색어로 일치를 판정한다.
- Then: 검색어가 없는 것으로 보고 모든 항목이 표시된다.

### TC-SETTING-HOLIDAY-DOMAIN-012: 공휴일 항목은 적용 국가의 공휴일로만 구성한다

- 근거: `domain > 공휴일 항목 구성`
- Given: 한국과 미국 공휴일이 저장되어 있고 적용 국가가 테스트 데이터의 국가다.
- When: 공휴일 항목을 구성한다.
- Then: 테스트 데이터의 국가 공휴일만 항목이 된다.
- 테스트 데이터:

  | 적용 국가 | 항목이 되는 공휴일 |
  | --- | --- |
  | 한국 | 한국 |
  | 미국 | 미국 |
  | 한국, 미국 | 한국, 미국 |
  | 없음 | 없음 |

### TC-SETTING-HOLIDAY-DOMAIN-013: 일괄 선택은 적용 국가의 공휴일을 기준으로 계산한다

- 근거: `domain > 일괄 선택 기준`
- Given: 적용 국가가 한국이고, 쉬는 날이 아닌 한국 공휴일 하나와 쉬는 날이 아닌 미국 공휴일 하나가 저장되어 있으며, 미국 공휴일 이름이 숨김으로 저장되어 있다.
- When: 테스트 데이터의 일괄 선택을 실행한다.
- Then: 저장된 숨긴 공휴일 이름 목록이 테스트 데이터의 결과로 교체된다.
- 테스트 데이터:

  | 일괄 선택 | 숨긴 공휴일 이름 목록 |
  | --- | --- |
  | `전체 해제` | 한국 공휴일 이름 |
  | `쉬는 날만 선택` | 한국 공휴일 이름 |

### TC-SETTING-HOLIDAY-DOMAIN-014: 같은 이름의 공휴일은 연도가 달라도 하나의 항목으로 제공한다

- 근거: `domain > 공휴일 항목 구성`
- Given: 이름이 같은 공휴일이 서로 다른 연도에 저장되어 있다.
- When: SettingHoliday에 표시할 공휴일 항목을 구성한다.
- Then: 그 이름의 항목은 하나만 제공되고 표시 이름은 저장된 이름 그대로다.

### 작성하지 않는 이유

- `TC-SETTING-HOLIDAY-DOMAIN-001`, `TC-SETTING-HOLIDAY-DOMAIN-003`, `TC-SETTING-HOLIDAY-DOMAIN-004`는 공백만 다른 이름을 같은 이름으로 합치고 대표 표기를 고르는 규칙이 스펙에서 사라져 검증할 행동이 없어졌다. 남은 케이스의 ID를 옮기지 않으려고 번호는 비워 둔다.
