# Routine 목록·상세 배치 테스트 케이스

기준 스펙: [Routine 목록·상세 배치 스펙](../spec/client/routine-list-detail.md)

이 문서에서 `domain > 배치에 참여하는 화면`, `feature > 추가 진입 수단`, `feature > 함께 사용과 단독 사용` 절은 [목록·상세 배치 공통 스펙](../spec/client/list-detail-pane.md)이 소유한다. 나머지 케이스의 절은 기준 스펙이 소유한다.

표시 비율 조절의 공통 행동과 유지 기준은 [목록·상세 배치 공통 스펙](../spec/client/list-detail-pane.md)에 위임되어 있다.

## feature

### TC-ROUTINE-LIST-DETAIL-FEATURE-001: 목록과 상세를 함께 쓰는 환경에서는 두 영역을 함께 표시한다

- 근거: `feature > 함께 사용과 단독 사용`
- Given: 목록과 상세를 함께 사용할 수 있는 환경이다.
- When: 사용자가 루틴 주요 목적지에 진입한다.
- Then: 루틴 목록을 유지한 채 상세 영역에 루틴 추가가 함께 표시된다.
- 작성하지 않는 이유: 목록과 상세를 함께 표시하는 창 환경과 두 영역의 전환 이력을 함께 구동해야 관찰할 수 있어 현재 단위 테스트 환경에서는 결정적으로 검증할 수 없다. 창 크기와 여러 화면의 전환 이력을 제어할 수 있는 통합 UI 테스트 환경이 갖춰지면 자동화한다.

### TC-ROUTINE-LIST-DETAIL-FEATURE-002: 한 화면만 제공하는 환경에서는 현재 화면만 단독으로 표시한다

- 근거: `feature > 함께 사용과 단독 사용`
- Given: 목록과 상세를 함께 사용할 수 없는 환경이다.
- When: 사용자가 루틴 목록 또는 루틴 추가를 사용한다.
- Then: 현재 사용하는 화면만 단독으로 표시된다.
- 작성하지 않는 이유: 목록과 상세를 함께 표시하는 창 환경과 두 영역의 전환 이력을 함께 구동해야 관찰할 수 있어 현재 단위 테스트 환경에서는 결정적으로 검증할 수 없다. 창 크기와 여러 화면의 전환 이력을 제어할 수 있는 통합 UI 테스트 환경이 갖춰지면 자동화한다.

### TC-ROUTINE-LIST-DETAIL-FEATURE-003: 상세 영역에 루틴 추가가 놓이면 목록에서 추가 수단을 제공하지 않는다

- 근거: `feature > 추가 진입 수단`
- Given: 상세 영역에 루틴 추가가 놓여 있다.
- When: 사용자가 루틴 목록에서 실행할 수 있는 동작을 확인한다.
- Then: 루틴 추가 동작이 제공되지 않는다.

### TC-ROUTINE-LIST-DETAIL-FEATURE-004: 목록만 사용하는 환경에서는 목록에서 추가 수단을 제공한다

- 근거: `feature > 추가 진입 수단`
- Given: 상세 영역에 루틴 추가가 놓여 있지 않다.
- When: 사용자가 루틴 목록에서 실행할 수 있는 동작을 확인한다.
- Then: 루틴 추가 동작이 제공된다.

### TC-ROUTINE-LIST-DETAIL-FEATURE-005: 목록과 함께 표시되면 루틴 추가에서 뒤로가기 수단을 제공하지 않는다

- 근거: `feature > 함께 사용과 단독 사용`
- Given: 루틴 목록과 루틴 추가가 함께 표시되어 있다.
- When: 사용자가 루틴 추가에서 실행할 수 있는 동작을 확인한다.
- Then: 뒤로가기 동작이 제공되지 않는다.

### TC-ROUTINE-LIST-DETAIL-FEATURE-006: 단독으로 표시되면 루틴 추가에서 뒤로가기 수단을 제공한다

- 근거: `feature > 함께 사용과 단독 사용`
- Given: 루틴 추가가 단독으로 표시되어 있다.
- When: 사용자가 루틴 추가에서 실행할 수 있는 동작을 확인한다.
- Then: 뒤로가기 동작이 제공된다.

## domain

### TC-ROUTINE-LIST-DETAIL-DOMAIN-001: 루틴 목록에서 진입하지 않은 루틴 추가는 목록과 함께 표시하지 않는다

- 근거: `domain > 배치에 참여하는 화면`
- Given: 목록과 상세를 함께 사용할 수 있는 환경이다.
- When: 사용자가 루틴 목록이 아닌 화면에서 루틴 추가로 이동한다.
- Then: 루틴 추가를 목록·상세 배치에 참여시키지 않아 단독으로 표시한다.

### TC-ROUTINE-LIST-DETAIL-DOMAIN-002: 루틴 목록에서 이어 진입한 루틴 추가는 목록과 함께 표시한다

- 근거: `domain > 배치에 참여하는 화면`
- Given: 목록과 상세를 함께 사용할 수 있는 환경이다.
- When: 사용자가 루틴 목록에서 루틴 추가로 이동한다.
- Then: 루틴 추가를 목록·상세 배치의 상세 영역에 표시한다.

### TC-ROUTINE-LIST-DETAIL-DOMAIN-003: 다른 주요 목적지에 다녀오면 상세 영역의 루틴 추가는 제목과 설명이 비어 있는 상태로 시작한다

- 근거: `domain > 유지와 초기화 기준`
- Given: 목록과 상세를 함께 사용하고 있고 상세 영역의 루틴 추가에 사용자가 제목과 설명을 입력했다.
- When: 사용자가 공통 내비게이션에서 다른 주요 목적지로 이동했다가 루틴을 다시 선택한다.
- Then: 상세 영역의 루틴 추가는 제목과 설명이 비어 있다.

## 작성하지 않는 이유

두 영역의 표시 비율 조절과 조절 중 즉시 반영, 화면 재생성 후 비율 유지, 다른 주요 목적지에 다녀온 뒤와 앱 재실행 후 기본 비율 초기화는 실제 화면 크기와 실행 경계를 함께 판정해야 한다. 현재 테스트 환경에서는 결정적으로 자동화할 수 없으며, 화면 크기와 실행 경계를 제어·관찰할 수 있는 통합 UI 테스트 환경이 갖춰지면 자동화한다.
